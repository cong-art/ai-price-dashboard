# 价格历史外部持久化（GitHub 私有仓库）—— 配置示例
#
# 复制本文件为 `.env` 并按需填写后，在跑抓取/服务前加载它（见 README「历史外部持久化」）。
# 所有变量均可选：缺失 / 为空 时，历史自动降级为只写本地 data/history.json（旧版行为）。
#
# 1) GH_HISTORY_TOKEN —— GitHub Personal Access Token
#    需对该私有仓库有 Contents 读写权限。生成：GitHub → Settings → Developer settings →
#    Personal access tokens → Tokens (classic) → 勾选 repo 权限。
#    （若用 Fine-grained token：仓库访问选目标私有仓库，权限勾 Contents: Read and write）
GH_HISTORY_TOKEN=github_pat_xxxx

# 2) GH_HISTORY_REPO —— 目标私有仓库，格式 owner/repo（必须已存在）
GH_HISTORY_REPO=yourname/price-history

# 3) GH_HISTORY_PATH —— 仓库内保存历史采样的文件路径（可选，默认 history.json）
GH_HISTORY_PATH=history.json
